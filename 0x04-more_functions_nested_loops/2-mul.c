#include "holberton.h"
#include <stdio.h>
/**
 *mul - multiplicate two numbers
 *Return: 1 or 0 depending on is uppercase or not.
 *@a: First character to multiplicate
 *@b: Second character to multiplicate
 */
int mul(int a, int b)
{
	return (a * b);
}

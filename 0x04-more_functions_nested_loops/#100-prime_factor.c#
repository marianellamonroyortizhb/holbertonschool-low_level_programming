#include <stdio.h>
/**
 *main - print prime numbers
 *Return: 0.
 */
int main(void)
{
	long int i, prime;

	long int n = 24;

	for ((i = n / 2); i <= n; i--)
	{
		prime = n % i;

		if (prime == 0 && i <= n)
		{
			Project 0x04. C - More functions, more nested loops | Holberton Intranet
break;
		}
	}
	return (i);
}

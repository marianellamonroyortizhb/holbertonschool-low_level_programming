#include "holberton.h"
/**
 *print_sign- Determine the sign of the integer
 *Return: 0, 1,-1
 * @n: integer to examinate
 */

int print_sign(int n)
{
	if (n > 0)
	{
		_putchar(+);
		return (1);
	}
	else if (n == 0)
	{
		_putchar(0);
		return (0);
	}
	

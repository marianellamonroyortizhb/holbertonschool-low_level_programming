#include "holberton.h"
/**
 *print_rev - Print a string in reverse
 *
 *@s: Strign to evaluate
 *Return: 0 Success;
 */
int _strlen(char *s)
{
	int i = 0;

	while (s[i] != '\0')
	{
		i++;
	}
	return (i);
}